import 'package:flutter/material.dart';

class WeakMenuPage extends StatelessWidget {
  const WeakMenuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('苦手項目')),
    );
  }
}
